# PostgreSQL Configuration
# Used if not overridden by environment variables

DB_HOST=localhost
DB_PORT=5432
DB_NAME=NJ
DB_USER=postgres
DB_PASSWORD=admin

# Google Cloud Configuration
# GCP_SERVICE_ACCOUNT_JSON=/path/to/service-account.json
# Or use GCP_SERVICE_ACCOUNT_B64 with base64 encoded JSON
